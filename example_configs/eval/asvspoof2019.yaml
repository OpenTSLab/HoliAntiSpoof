defaults:
  - override hydra/job_logging: none

spoof_score_name: binary_prob

infer_dir: "experiments/holi_anti_spoof/infer_step20000"

pred_durations:
  - pred: ${infer_dir}/asvspoof2019_all.json

output_file: ${infer_dir}/results/asvspoof2019_all.txt


hydra:
  output_subdir: null
  run:
    dir: .
